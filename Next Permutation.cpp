#include <bits/stdc++.h> 
vector<int> nextPermutation(vector<int> &permutation, int n)
{
    //  Write your code here.
     bool res=next_permutation(permutation.begin(), permutation.end());
     return permutation;
}